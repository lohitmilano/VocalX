fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
pillow==11.0.0
numpy==2.1.3

# ML
torch>=2.2.0
torchaudio>=2.2.0
transformers>=4.47.0
accelerate>=0.33.0
safetensors>=0.4.5

# SAM-Audio python package is required for `/sam_audio/separate`.
# Install it per the official SAM-Audio docs (may require a git install / gated access).
# sam-audio

# Memory monitoring for RAM checks
psutil>=6.0.0




